<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.El_gordon">

        <activity android:name=".ResultadoNivel1Activity" android:exported="false" />
        <activity android:name=".ResultadoNivel2Activity" android:exported="false" />
        <activity android:name=".ResultadoNivel3Activity" android:exported="false" />


        <!-- Última pantalla (el juego) -->
        <activity android:name=".JuegoActivity" android:exported="false" />

        <!-- Pantalla de recetas -->
        <activity android:name=".RecipeSelector" android:exported="false" />

        <!-- Pantalla de niveles -->
        <activity android:name=".LevelSelector" android:exported="false" />

        <!-- Pantalla de selección de avatar -->
        <activity android:name=".AvatarSelector" android:exported="false" />

        <!-- Pantalla de inicio (primera que se abre) -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
