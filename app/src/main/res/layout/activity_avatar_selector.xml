<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/kitchen_background"
    tools:context=".AvatarSelector">

    <!-- Flecha izquierda -->
    <ImageButton
        android:id="@+id/btnLeft"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:background="@android:color/transparent"
        android:src="@drawable/arrow_icon256"
        android:contentDescription="flecha_arriba"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/imageCarousel"
        android:layout_marginEnd="16dp"
        android:scaleType="fitCenter"
        android:rotation="-90"
        android:scaleX="-1"/>

    <!-- Imagen del carrusel -->
    <ImageView
        android:id="@+id/imageCarousel"
        android:layout_width="350dp"
        android:layout_height="500dp"
        android:scaleType="centerCrop"
        android:src="@drawable/chef1_icon"
        android:contentDescription="imagen_carrusel"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/btnLeft"
        app:layout_constraintEnd_toStartOf="@id/btnRight" />

    <!-- Flecha derecha -->
    <ImageButton
        android:id="@+id/btnRight"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:background="@android:color/transparent"
        android:src="@drawable/arrow_icon256"
        android:contentDescription="flecha_abajo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/imageCarousel"
        android:layout_marginStart="16dp"
        android:scaleType="fitCenter"
        android:rotation="90"/>


</androidx.constraintlayout.widget.ConstraintLayout>
